To secure the **10 marks** allocated for the **README & Documentation** component of your project , your `README.md` file needs to be structured so that an evaluator can clone your repository, understand your code architecture, install the required packages, and launch the application in under 10 minutes. It must also explicitly detail your design decisions and model tuning process.

Here is a fully compiled, professionally structured `README.md` written explicitly for your project. You can copy this text block directly, save it as `README.md` in your main project folder, and push it to your GitHub repository.

---

```markdown
# AI vs. Human Text Detection Dashboard

[cite_start]An end-to-end Machine Learning and Deep Learning classification framework built to distinguish between human-written text and text generated by Artificial Intelligence[cite: 6]. [cite_start]This production-ready system analyzes stylistic patterns, word frequencies, and semantic contextual cues to classify text files with real-time probabilistic evaluations[cite: 7, 20].

**Live Application Link:** [Insert Your Deployed Streamlit App URL Here]

---

## 📂 Project Architecture

[cite_start]The project repository strictly follows the required architecture schema to isolate source data sets, notebook checkpoints, deployment script files, and optimized binary model weights[cite: 73, 74]:

```text
ai_human_detection_project/
├── app.py                         # Main Streamlit application file
├── requirements.txt               # Documented runtime environmental dependencies
├── README.md                      # Comprehensive setup, deployment, and configuration guide
├── models/                        # Serialized feature maps and optimized model files
│   ├── svm_model.pkl              # Support Vector Machine classifier
│   ├── decision_tree_model.pkl    # Decision Tree classifier
│   ├── adaboost_model.pkl         # Optimized AdaBoost ensemble classifier
│   ├── fnn_model.h5               # Feedforward Neural Network weights
│   ├── lstm_model.h5              # Long Short-Term Memory model weights
│   ├── cnn_model.h5               # 1D Convolutional Neural Network weights
│   └── tfidf_vectorizer.pkl       # Formatted TF-IDF vector text mapping configuration
├── data/                          # Input data caches (ignored or tracked locally)
│   ├── training_data/
│   └── test_data/
└── notebooks/
    └── project1_notebook.ipynb    # Main development, tuning, and evaluation notebook

```

---

## ⚡ Quick Start: Installation & Local Deployment

Follow these three commands to set up your environment locally in under 10 minutes:

### 1. Clone the Workspace

```bash
git clone [https://github.com/LeoLakshman/ai_human_detection_project.git](https://github.com/LeoLakshman/ai_human_detection_project.git)
cd ai_human_detection_project

```

### 2. Configure Dependencies inside a Virtual Environment

```bash
# Create a localized environment block using Python 3.11
python -m venv venv
source venv/bin/activate  # On Windows use: venv\Scripts\activate

# Install compiled wheel files and core libraries
pip install --upgrade pip
pip install -r requirements.txt

```

### 3. Initialize the Streamlit Server

```bash
streamlit run app.py

```

---

## 🛠️ Feature Engineering & Model Tuning

### 1. Linguistic Feature Architecture

Before feeding text strings into classifiers, numbers are extracted across three distinct layers:

* 
**TF-IDF Representations:** Maps unique token distinctiveness matrices over a 5,000-column boundary space.


* 
**Sequential Embeddings:** Processes contextual semantics for our deep learning networks via deep vocabulary tokens.


* 
**Handcrafted Linguistic Features:** Measures sentence structural distribution shifts, word length token properties, and overall type-token vocabulary richness ratio (TTR) changes.



### 2. Hyperparameter Optimization Metrics

Six distinct algorithms are evaluated side-by-side inside this system:

* 
**Support Vector Machine (SVM):** Tuned using `GridSearchCV` over parameters `C` and `kernel` space to isolate optimal linear separators on sparse vectors.


* 
**Decision Tree:** Configured with constrained max-depth parameters to establish interpretable text rules without risking over-fitting.


* 
**AdaBoost Ensemble:** Upgraded using an explicit shallow estimator base ($max\_depth=3$) combined with a balanced learning rate ($0.1$) to build higher-confidence boosting iteration bounds over tough classification points.


* 
**Feedforward NN (FNN):** A multi-layer architecture handling dense inputs, integrated with a steady optimizer learning rate ($0.0005$), dropout regularization boundaries ($0.4$), and automated `EarlyStopping` callbacks to keep weight updates clean.


* 
**LSTM Networks:** Reads inputs sequentially to map out stylistic patterns and maintain long-range document context dependencies.


* 
**1D CNN for Text:** Leverages expanded phrase filter feature banks ($filters=256$) combined with global max-pooling to pick up local phrase clusters.



---

## 💡 Production Design Decisions

* **Cloud Asset-Streaming Strategy:** Pushing massive binary tensor and pickle files directly over standard Git tracking loops causes browser timeouts and slows down repository version histories. To bypass this storage limitation seamlessly, all 8 trained artifacts are stored as secure binary assets under a **GitHub Release tag (v1.0.0)**. The `app.py` script utilizes an on-the-fly downloader cache system via `@st.cache_resource` that streams and indexes models directly into memory upon server spin-up.
* **Environment Versioning Fixes:** Addressed cross-platform serialization scoping mismatches during migration between Google Colab training and Streamlit Cloud environments by writing an automated attribute patch handler (`_effective_probability = True`) onto the wrapper objects right after loading, preventing downstream execution runtime faults.

```

***

### What to do after completing this step:
1. Create a plain text file named `deployment_links.txt` containing your active Streamlit Web URL, your public GitHub Repository Link, and your Demo Video Link.
2. Ensure your fully executed training notebook (`project1_notebook.ipynb`), your `app.py`, your `requirements.txt`, the dataset, your `README.md`, and that links text file are all in your local folder.
3. Compress everything into a single file named `submission_package.zip` (remember to **exclude** the heavy files inside the `models/` folder from your zip file so it uploads instantly to your university portal) and submit!

```
